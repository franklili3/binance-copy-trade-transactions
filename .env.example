# =============================================================================
# 币安带单交易记录获取器 - 配置文件模板
# =============================================================================
# 复制此文件为 .env 并填入你的实际配置信息
# 请勿将 .env 文件提交到版本控制系统

# =============================================================================
# API 配置 (必需)
# =============================================================================
# 从币安官网获取API密钥: https://www.binance.com/cn/my/settings/api-management
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_key_here

# =============================================================================
# 环境设置
# =============================================================================
# 是否使用测试网 (true/false)
# 测试网申请: https://testnet.binance.vision/
BINANCE_TESTNET=false

# =============================================================================
# 交易配置规则
# =============================================================================

# 主要交易对 (用逗号分隔，无空格)
# 程序会优先获取这些交易对的交易记录
MAJOR_SYMBOLS=BTC/USDT,ETH/USDT,BNB/USDT,ADA/USDT,SOL/USDT

# 默认分析天数
DEFAULT_ANALYSIS_DAYS=30

# 默认开始日期 (YYYY-MM-DD格式)
# 如果不指定，将使用2025-04-01作为默认开始日期
DEFAULT_START_DATE=2025-04-01

# =============================================================================
# 数据处理规则
# =============================================================================

# 收益率限制范围 (小数形式)
# 收益率将被限制在此范围内以避免异常值
MAX_RETURN_RATE=0.20
MIN_RETURN_RATE=-0.20

# 初始资金假设 (USDT)
# 用于计算投资组合价值时的初始资金
INITIAL_USDT_BALANCE=10000.0

# =============================================================================
# 输出文件配置
# =============================================================================

# 输出文件前缀
OUTPUT_PREFIX=pyfolio

# 是否保存调试日志 (true/false)
SAVE_DEBUG_LOG=true

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# 高级配置
# =============================================================================

# API请求限制 (每分钟请求数)
RATE_LIMIT=1200

# 请求超时时间 (秒)
REQUEST_TIMEOUT=30

# 最大重试次数
MAX_RETRIES=3

# =============================================================================
# 资产价格估算 (USDT)
# 用于无法获取实时价格时的估算值
# =============================================================================
BTC_ESTIMATED_PRICE=95000.0


# =============================================================================
# 使用说明
# =============================================================================
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 在 .env 文件中填入你的实际配置
# 3. 确保 .env 文件已添加到 .gitignore 中
# 4. 运行程序: python binance_transactions.py
#
# 注意事项:
# - API密钥需要有现货交易和读取信息的权限
# - 生产环境的API密钥请谨慎使用，建议先在测试网测试
# - 定期更新API密钥以确保安全
# - 监控API使用量，避免超过限制
# =============================================================================
